export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  MYPAGE: '/mypage',

  // 동적 경로 — 프로젝트에 맞게 추가
  // productDetail: (id: string) => `/products/${id}`,
  // orderDetail: (id: string) => `/orders/${id}`,
} as const;
